import 'package:flutter/material.dart';
import 'package:fresh_cut_dagmawi/ui/components/root_wrapper.dart';

// ignore_for_file: non_constant_identifier_names
Widget NavigationView() => const RootWrapper(
      child: Center(
        child: Text("Not in Scope"),
      ),
    );
Widget HomeView() => RootWrapper(
      child: Center(
        child: Text("Not in Scope"),
      ),
    );
Widget ChatView() => const RootWrapper(
      child: Center(
        child: Text("Not in Scope"),
      ),
    );
Widget ProfileView() => const RootWrapper(
      child: Center(
        child: Text("Not in Scope"),
      ),
    );
