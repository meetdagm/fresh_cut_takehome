import 'package:flutter/material.dart';
import 'package:fresh_cut_dagmawi/ui/shared/styles.dart';
// import 'package:palette_generator/palette_generator.dart';

class ImageColorExtractor {
  // Future<Color> extractPrimaryColorFromImage() async {
      //We can use a library such as Palette generator to extract the primary color given an image as input
  //   return await PaletteGenerator.fromImageProvider(image).primary
  // }
}
